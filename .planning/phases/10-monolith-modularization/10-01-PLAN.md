---
phase: 10-monolith-modularization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - state.py
  - models.py
  - helpers.py
  - routers/__init__.py
autonomous: true
requirements: [INFRA-03]

must_haves:
  truths:
    - "config.py contains all configuration constants previously in main.py lines 56-126"
    - "state.py contains _ws_connections, _network_ws_clients, _embed_model, _embed_lock, _auth_rate_limits"
    - "models.py contains all 60+ Pydantic BaseModel subclasses previously scattered through main.py"
    - "helpers.py contains all underscore-prefixed helper functions that are used across multiple router domains"
    - "routers/ package directory exists with __init__.py"
    - "main.py still runs and all tests pass after this plan (no routes moved yet, just new files created)"
  artifacts:
    - path: "config.py"
      provides: "All configuration constants and env var loading"
      contains: "TIER_RATE_LIMITS"
    - path: "state.py"
      provides: "Shared mutable state for WebSocket connections and embedding model"
      contains: "_ws_connections"
    - path: "models.py"
      provides: "All Pydantic request/response models"
      contains: "class SignupRequest"
    - path: "helpers.py"
      provides: "Shared helper functions used across routers"
      contains: "_queue_email"
    - path: "routers/__init__.py"
      provides: "Router package marker"
  key_links:
    - from: "config.py"
      to: "main.py"
      via: "import constants"
      pattern: "from config import"
    - from: "state.py"
      to: "main.py"
      via: "import shared state"
      pattern: "from state import"
---

<objective>
Extract shared infrastructure from main.py (6752 lines) into four foundation modules: config.py, state.py, models.py, and helpers.py. These modules will be imported by both main.py and the router modules created in Plan 02.

Purpose: Create the dependency foundation that router modules will import. Without these, routers would have circular imports back to main.py.
Output: Four new Python modules + routers/ package directory. main.py updated to import from new modules instead of defining inline. All tests still pass.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@main.py
@test_main.py
@db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create config.py, state.py, and models.py</name>
  <files>config.py, state.py, models.py, routers/__init__.py</files>
  <action>
1. Create `routers/__init__.py` as empty file (package marker).

2. Create `config.py` by extracting from main.py lines 56-126:
   - All constants: MAX_MEMORY_VALUE_SIZE, MAX_QUEUE_PAYLOAD_SIZE, RATE_LIMIT_WINDOW, RATE_LIMIT_MAX, TIER_RATE_LIMITS, TIER_LIMITS
   - Admin config: ADMIN_PASSWORD_HASH, ADMIN_SESSION_TTL
   - Encryption: ENCRYPTION_KEY, _fernet
   - JWT config: JWT_SECRET, JWT_ALGORITHM, JWT_EXPIRY_DAYS, MOLTBOOK_SERVICE_KEY
   - Stripe: STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PRICE_HOBBY, STRIPE_PRICE_TEAM, STRIPE_PRICE_SCALE
   - SMTP: SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS (find these in the email section ~line 3780+)
   - TURNSTILE: TURNSTILE_SECRET_KEY and TURNSTILE_URL
   - Import all needed stdlib: os, secrets, logging
   - Import cryptography.fernet for _fernet
   - Keep the same env var loading logic and fallback behavior
   - Export `logger` (the moltgrid logger instance)

3. Create `state.py`:
   - `_ws_connections: dict[str, set] = {}` (WebSocket type hint would create import cycle, use set)
   - `_network_ws_clients: list = []`
   - `_embed_model = None`
   - `_embed_lock = threading.Lock()`
   - `_auth_rate_limits: dict = {}`
   - Import threading

4. Create `models.py` by extracting ALL Pydantic BaseModel subclasses from main.py:
   - There are 60+ model classes. Extract every single one.
   - Keep the same import: `from pydantic import BaseModel, ConfigDict, Field`
   - Keep `from typing import Optional, List, Union`
   - Models to extract (complete list from grep): SignupRequest, LoginRequest, ForgotPasswordRequest, ResetPasswordRequest, TOTP2FAVerifyRequest, TOTP2FADisableRequest, NotificationPreferencesRequest, MemoryVisibilityRequest, MemoryBulkVisibilityRequest, TransferRequest, IntegrationCreateRequest, IntegrationStatusItem, IntegrationStatusResponse, UserScheduleRequest, UserScheduleUpdateRequest, WebhookRegisterRequest, WebhookResponse, CheckoutRequest, RegisterRequest, RegisterResponse, MoltBookEventRequest, MoltBookRegisterRequest, OnboardingResponse, HeartbeatRequest, MemorySetRequest, MemoryGetResponse, MemoryKeyEntry, MemoryListResponse, HealthStatsResponse, HealthResponse, QueueJobEntry, QueueListResponse, ScheduleEntry, ScheduleListResponse, QueueSubmitRequest, QueueJobResponse, QueueFailRequest, RelayMessage, TextProcessRequest, ScheduledTaskRequest, ScheduledTaskResponse, VectorUpsertRequest, VectorSearchRequest, SharedMemorySetRequest, DirectoryUpdateRequest, StatusUpdateRequest, CollaborationRequest, MarketplaceCreateRequest, MarketplaceDeliverRequest, MarketplaceReviewRequest, ScenarioCreateRequest, PubSubSubscribeRequest, PubSubPublishRequest, AdminLoginRequest, SessionCreateRequest, SessionAppendRequest, ContactForm, OrgCreateRequest, OrgInviteRequest, OrgRoleUpdateRequest, EventAckRequest, ObstacleCourseSubmitRequest

5. Do NOT modify main.py yet in this task. The new files are additive only.
  </action>
  <verify>
    <automated>python -c "from config import TIER_RATE_LIMITS, JWT_SECRET, STRIPE_SECRET_KEY; from state import _ws_connections, _embed_model; from models import SignupRequest, MemorySetRequest, HealthResponse; print('All imports OK')"</automated>
  </verify>
  <done>config.py, state.py, models.py, and routers/__init__.py all exist and are importable without errors. Every constant and model class is present.</done>
</task>

<task type="auto">
  <name>Task 2: Create helpers.py with shared helper functions</name>
  <files>helpers.py</files>
  <action>
Create `helpers.py` by extracting helper functions from main.py that are called from multiple route domains (not single-domain helpers that will move with their router).

**Functions to extract (cross-cutting helpers):**
- `hash_key(key)` (line 296) — used by auth, dashboard, register
- `generate_api_key()` (line 299) — used by auth, dashboard, register
- `_track_event(event_name, ...)` (line 302) — used everywhere
- `_check_usage_quota(db, agent_id)` (line 320) — used by memory, queue, relay, etc.
- `_check_auth_rate_limit(request)` (line 128) — used by middleware
- `get_agent_id(request)` (line 380) — FastAPI Depends, used by ALL agent endpoints
- `get_user_id(request)` (line 455) — FastAPI Depends, used by ALL user endpoints
- `get_optional_user_id(request)` (line 462) — used by some endpoints
- `_create_token(user_id, email)` (line 438) — used by auth, register
- `_decode_token(token)` (line 447) — used by get_user_id
- `_verify_turnstile(token)` (line 477) — used by auth signup, contact, register
- `_encrypt(plaintext)` (line 278) — used by memory endpoints
- `_decrypt(ciphertext)` (line 284) — used by memory endpoints
- `_verify_agent_ownership(db, agent_id, user_id)` (line 866) — used by dashboard endpoints
- `_log_memory_access(action, ...)` (line 2684) — used by memory, dashboard
- `_log_audit(action, ...)` (line 2708) — used by auth, dashboard, admin
- `_queue_agent_event(agent_id, event_type, payload)` (line 2731) — used by memory, queue, relay
- `_check_memory_visibility(db, ...)` (line 2748) — used by memory, dashboard
- `_branded_email(title, body_content)` (line 3710) — used by auth, billing, etc.
- `_queue_email(to_email, subject, body_html)` (line 3753) — used everywhere
- `_get_client_ip(request)` (line 3775) — used by auth, middleware
- `_sanitize_text(text)` (line 2161) — used by register, relay
- `_fire_webhooks(agent_id, event_type, data)` (line 3398) — used by memory, queue, relay, schedules
- `_is_safe_url(url)` (line 3289) — used by webhooks
- `_http_code_to_slug(status)` (line 201) — used by exception handlers
- `_apply_tier(db, user_id, tier)` (line 1912) — used by billing
- `_get_or_create_stripe_customer(db, user_id, email)` (line 1921) — used by billing
- `_tier_from_price(price_id)` (line 1996) — used by billing
- `_get_embed_model()` — used by vector endpoints
- `_check_onboarding_progress(db, agent_id)` (line 2450) — used by onboarding

**Imports in helpers.py:**
- `from config import *` (all config constants)
- `from state import _ws_connections, _auth_rate_limits, _embed_model, _embed_lock`
- `from db import get_db, get_standalone_conn, DB_PATH, DB_BACKEND`
- Standard lib: os, json, time, uuid, hashlib, hmac, secrets, threading, logging, re, html, smtplib, ipaddress, socket, base64, io, numpy
- Third-party: httpx, jwt (pyjwt), bcrypt, stripe, pyotp, croniter, sentence_transformers, urllib.parse, qrcode
- FastAPI: HTTPException, Header, Depends, Query, Request

**Background loop functions to extract:**
- `_webhook_delivery_loop()` + `_run_webhook_delivery_tick()` (lines 3419-3460)
- `_scheduler_loop()` + `_run_scheduler_tick()` (lines 3587-3630)
- `_uptime_loop()` + `_uptime_check()` + `_run_liveness_check()` + `_liveness_loop()` (lines 3631-3690)
- `_usage_reset_loop()` + `_run_usage_reset()` (lines 3690-3710)
- `_email_loop()` (line 3810+)

These background functions are imported by main.py's lifespan and by test_main.py.

Do NOT modify main.py yet. helpers.py is additive.
  </action>
  <verify>
    <automated>python -c "from helpers import hash_key, generate_api_key, get_agent_id, get_user_id, _fire_webhooks, _queue_email, _check_memory_visibility, _log_memory_access, _run_scheduler_tick, _run_liveness_check, _run_webhook_delivery_tick, _branded_email; print('All helper imports OK')"</automated>
  </verify>
  <done>helpers.py exists and is importable. All cross-cutting helper functions and background loop functions are present and correctly import from config.py, state.py, and db.py.</done>
</task>

</tasks>

<verification>
- `python -c "import config; import state; import models; import helpers; print('All 4 modules importable')"` passes
- `pytest test_main.py -x --tb=short` still passes (main.py unchanged, new files are additive only)
- Each new module can be imported independently without circular import errors
</verification>

<success_criteria>
- config.py contains all configuration constants from main.py
- state.py contains all shared mutable state variables
- models.py contains all 60+ Pydantic model classes
- helpers.py contains all cross-cutting helper functions and background loops
- routers/ directory exists with __init__.py
- All existing tests still pass (main.py untouched)
- No circular imports between the new modules
</success_criteria>

<output>
After completion, create `.planning/phases/10-monolith-modularization/10-01-SUMMARY.md`
</output>
