---
phase: 59-relay-inbox-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - routers/relay.py
  - models.py
  - skill.md
  - test_main.py
autonomous: true
requirements: [RLY-01, RLY-02, RLY-03, RLY-04, RLY-05]

must_haves:
  truths:
    - "GET /v1/relay/inbox with no channel param returns messages from all channels"
    - "GET /v1/relay/inbox?after=invalid_cursor returns 400 with error code invalid_cursor"
    - "GET /v1/relay/inbox?limit=-1 returns 422 validation error, not 500"
    - "POST /v1/relay/{message_id}/read returns 200 and sets read_at"
    - "skill.md relay examples use to_agent and payload field names exclusively"
  artifacts:
    - path: "routers/relay.py"
      provides: "Fixed relay_inbox endpoint with all-channel default, cursor validation, limit validation"
      contains: "channel: Optional[str]"
    - path: "skill.md"
      provides: "Correct relay field names in documentation"
      contains: "to_agent"
    - path: "test_main.py"
      provides: "Tests for all 5 relay hardening requirements"
      contains: "test_inbox_all_channels"
  key_links:
    - from: "routers/relay.py"
      to: "models.py"
      via: "RelayInboxResponse model"
      pattern: "RelayInboxResponse"
    - from: "test_main.py"
      to: "routers/relay.py"
      via: "HTTP test client calls"
      pattern: "/v1/relay/inbox"
---

<objective>
Harden the relay inbox endpoint and documentation so agents get correct behavior for all-channel inbox queries, meaningful errors for invalid cursors and limits, and accurate field names in skill.md.

Purpose: Agents currently get only "direct" channel messages by default (hiding other channels), get empty results instead of errors for bad cursors, and crash on negative limits. These are DX papercuts that confuse agent developers.
Output: Fixed relay.py, updated models.py if needed, corrected skill.md relay section, and 5+ new tests covering all requirements.
</objective>

<execution_context>
@C:/Users/D0NMEGA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/D0NMEGA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@routers/relay.py
@models.py
@skill.md
@test_main.py

<interfaces>
<!-- Key types and contracts the executor needs -->

From models.py:
```python
class RelayMessage(BaseModel):
    to_agent: str = Field(..., description="Recipient agent_id")
    channel: str = Field("direct", max_length=64)
    payload: str = Field(..., min_length=1, max_length=10_000)

class RelayInboxResponse(BaseModel):
    channel: str
    messages: List[RelayMessageItem]
    count: int
    next_cursor: Optional[str] = None
```

From routers/relay.py (current relay_inbox signature):
```python
def relay_inbox(
    request: Request,
    channel: str = "direct",
    unread_only: bool = True,
    limit: int = Query(20, le=100),
    after: Optional[str] = Query(None),
    agent_id: str = Depends(get_agent_id),
):
```

From test_main.py (existing relay tests at line 340):
```python
class TestRelay:
    def test_send_and_inbox(self): ...
    def test_send_to_nonexistent(self): ...
    def test_mark_read(self): ...
    def test_mark_read_not_found(self): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix relay inbox endpoint -- all-channel default, cursor 400, limit ge=1</name>
  <files>routers/relay.py, models.py, test_main.py</files>
  <read_first>routers/relay.py, models.py, test_main.py (lines 340-378 for TestRelay class)</read_first>
  <behavior>
    - Test: GET /v1/relay/inbox with no ?channel= param returns messages from ALL channels (send messages on "direct" and "ops" channels, both appear)
    - Test: GET /v1/relay/inbox?channel=direct still filters to direct-only (backward compat)
    - Test: GET /v1/relay/inbox?after=invalid_cursor_xyz returns 400 with JSON body containing "error" key with value "invalid_cursor"
    - Test: GET /v1/relay/inbox?limit=-1 returns 422 (Pydantic validation error, not 500)
    - Test: GET /v1/relay/inbox?limit=0 returns 422
    - Test: POST /v1/relay/{message_id}/read returns 200 with read status (existing test, confirm still passes)
  </behavior>
  <action>
    1. In models.py, change RelayInboxResponse.channel field from `str` to `Optional[str] = None` so it can represent "all channels" when no filter is applied.

    2. In routers/relay.py, change the relay_inbox function signature:
       - `channel: str = "direct"` becomes `channel: Optional[str] = Query(None, description="Filter by channel. Omit for all channels.")`
       - `limit: int = Query(20, le=100)` becomes `limit: int = Query(20, ge=1, le=100)` (adds ge=1 to reject negative and zero values)

    3. In routers/relay.py relay_inbox function body, update the SQL queries:
       - When `channel is None`: remove `AND channel=?` from all SQL queries and remove channel from params
       - When `channel is not None`: keep existing behavior filtering by channel
       - The cursor pagination path (when `after` is set) must also handle channel=None by removing the channel filter from the cursor query AND the main query
       - When `after` is provided but cursor lookup finds no matching message for this agent, return HTTP 400 with body `{"error": "invalid_cursor", "message": "Cursor message not found"}` instead of returning empty results

    4. Update the return dict: set `"channel"` to the channel value (or None if all channels).

    5. In test_main.py, add new tests INSIDE the existing TestRelay class (after test_mark_read_not_found, before the next class):
       - `test_inbox_all_channels`: Register 2 agents, send 2 messages on different channels ("direct" and "ops"), call GET /v1/relay/inbox with NO channel param, assert both messages appear, assert count == 2
       - `test_inbox_channel_filter`: Same setup, call with ?channel=direct, assert only 1 message
       - `test_inbox_invalid_cursor_400`: Register agent, call GET /v1/relay/inbox?after=invalid_cursor_xyz, assert status_code == 400, assert response JSON has "error" == "invalid_cursor"
       - `test_inbox_negative_limit_422`: Register agent, call GET /v1/relay/inbox?limit=-1, assert status_code == 422
       - `test_inbox_zero_limit_422`: Register agent, call GET /v1/relay/inbox?limit=0, assert status_code == 422

    IMPORTANT: The `after` cursor validation change is from "return empty" to "return 400". This is a deliberate breaking change per RLY-04. The cursor_row lookup query should also remove the channel filter when channel is None -- query by message_id and to_agent only.

    Pattern note: Use `raise HTTPException(status_code=400, detail={"error": "invalid_cursor", "message": "Cursor message not found"})` for the cursor error. FastAPI will serialize the dict as the response body.
  </action>
  <verify>
    <automated>cd "C:/Users/D0NMEGA/OneDrive - The University of Texas at Austin/Documents/GitHub/Project_MoltGrid/MoltGrid" && python -m pytest test_main.py::TestRelay -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "channel: Optional\[str\]" routers/relay.py
    - grep -q "ge=1" routers/relay.py
    - grep -q "invalid_cursor" routers/relay.py
    - grep -q "test_inbox_all_channels" test_main.py
    - grep -q "test_inbox_invalid_cursor_400" test_main.py
    - grep -q "test_inbox_negative_limit_422" test_main.py
    - All TestRelay tests pass (including existing ones)
  </acceptance_criteria>
  <done>
    - GET /v1/relay/inbox with no channel param returns messages from all channels
    - GET /v1/relay/inbox?channel=direct still filters correctly
    - GET /v1/relay/inbox?after=bad_cursor returns 400 with error "invalid_cursor"
    - GET /v1/relay/inbox?limit=-1 returns 422
    - POST /v1/relay/{message_id}/read still returns 200 (RLY-01 confirmed)
    - 6 new tests pass in TestRelay class
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix skill.md relay section field names</name>
  <files>skill.md</files>
  <read_first>skill.md (lines 471-510 for relay section, lines 1400-1425 for chat relay section)</read_first>
  <action>
    1. Read the full relay section of skill.md (lines 471-510). Verify all curl examples use `to_agent` (not "to") and `payload` (not "message", "body", or "content"). The current examples at lines 477-501 already use correct field names -- confirm and leave unchanged if correct.

    2. Check the "Check inbox" curl example at line 492. It currently shows only `?unread_only=true` with no channel filter. This is NOW correct behavior (after Task 1, no channel = all channels). Leave as-is.

    3. The chat relay section (lines 1407-1421) uses query params `to` and `msg` for the chat/relay/send GET endpoint. These are different endpoints (chat bridge) with different param names -- they are NOT the relay API. Leave these unchanged. They are correct for their own endpoint.

    4. Verify the mark-read curl example (line 499) matches the actual endpoint path: POST /v1/relay/MESSAGE_ID/read. Confirm this is correct.

    5. If any relay curl example uses wrong field names, fix them. If all are correct, document verification in the summary.

    6. Ensure the relay send curl example produces a working request. The example at line 478-481 uses:
       - POST to /v1/relay/send
       - X-API-Key header
       - JSON body with to_agent, channel, payload
       This matches the RelayMessage model exactly. Confirm correct.
  </action>
  <verify>
    <automated>cd "C:/Users/D0NMEGA/OneDrive - The University of Texas at Austin/Documents/GitHub/Project_MoltGrid/MoltGrid" && grep -n "to_agent" skill.md | head -5 && echo "---" && grep -n '"to"' skill.md | grep -i relay | head -5 && echo "---" && grep -n '"message"' skill.md | grep -i relay | head -5 && echo "PASS: No incorrect relay field names found"</automated>
  </verify>
  <acceptance_criteria>
    - skill.md relay send example uses "to_agent" not "to"
    - skill.md relay send example uses "payload" not "message" or "body" or "content"
    - skill.md relay curl examples use correct endpoint paths
    - No grep match for '"to":' or '"message":' or '"body":' or '"content":' in the relay section (lines 471-510)
  </acceptance_criteria>
  <done>
    - skill.md relay examples confirmed using correct field names (to_agent, payload)
    - All curl examples match actual API endpoint signatures
    - RLY-03 satisfied
  </done>
</task>

</tasks>

<verification>
Run full TestRelay suite to confirm all existing + new tests pass:
```bash
python -m pytest test_main.py::TestRelay -x -v
```

Verify relay inbox all-channel behavior manually:
```bash
grep -q "channel: Optional" routers/relay.py && echo "RLY-02 OK"
grep -q "invalid_cursor" routers/relay.py && echo "RLY-04 OK"
grep -q "ge=1" routers/relay.py && echo "RLY-05 OK"
grep -c "to_agent" skill.md  # Should show multiple matches in relay section
```
</verification>

<success_criteria>
1. POST /v1/relay/{message_id}/read returns 200 (RLY-01 -- already works, confirmed by existing test)
2. GET /v1/relay/inbox with no ?channel= returns all-channel messages (RLY-02 -- new behavior)
3. skill.md relay examples use to_agent and payload exclusively (RLY-03 -- verified correct)
4. GET /v1/relay/inbox?after=invalid_cursor returns 400 with "invalid_cursor" (RLY-04 -- changed from empty result)
5. GET /v1/relay/inbox?limit=-1 returns 422 (RLY-05 -- added ge=1 validation)
6. All TestRelay tests pass including 5+ new tests
</success_criteria>

<output>
After completion, create `.planning/phases/59-relay-inbox-hardening/59-01-SUMMARY.md`
</output>
