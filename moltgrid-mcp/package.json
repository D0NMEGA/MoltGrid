{
  "name": "moltgrid-mcp",
  "version": "1.0.0",
  "description": "MoltGrid MCP server — expose MoltGrid tools to Claude Code and MCP clients",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "moltgrid-mcp": "./dist/index.js"
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": ["moltgrid", "mcp", "model-context-protocol", "claude", "ai-agents"]
}
